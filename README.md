# terraform-plugin-framework-internal

Internal libraries for the next-gen framework used to build Terraform providers.

```bash
go mod init terraform-plugin-framework-internal
```

```bash
go mod tidy
```